# Database Seed / Reset — [Project Name]
# Claude fills this during /new-project based on the chosen DB and ORM.
# Read this file before seeding or resetting the database.

## Seed (Populate With Test Data)
[Claude fills this — e.g., `python manage.py seed` or `npm run db:seed` or `psql < seeds/dev.sql`]

## Reset (Wipe and Re-seed From Scratch)
[Claude fills this — e.g., drop + recreate + migrate + seed commands in order]

## Required Environment Variables
[Claude fills this — DB connection string, any secrets the seed script needs]

## What Gets Created
[Claude fills this — describe the test data: users, sample records, etc.]

## Warning: Never Run Against Production
[Claude fills this — describe any safeguards in place, e.g., script checks NODE_ENV != production]
