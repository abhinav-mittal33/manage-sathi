# Deploy — [Project Name]
# Claude fills this during /new-project based on the chosen deployment target.
# Read this file before deploying. Follow every step in order.

## Pre-deploy Checklist
[Claude fills this — e.g., run tests, check env vars, build succeeds, migrations reviewed]

## Deploy Command
[Claude fills this — e.g., `git push heroku main` or `vercel --prod` or `fly deploy`]

## Environment Variables Required in Production
[Claude fills this — list every var that must be set in the prod environment]

## Post-deploy Verification
[Claude fills this — e.g., hit /health endpoint, check logs for errors, smoke test key flows]

## Rollback Procedure
[Claude fills this — e.g., `heroku rollback` or redeploy previous git sha or restore DB backup]
